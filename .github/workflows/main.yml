name: Ask2God CI

on:
  pull_request:
    branches: [ master ]
  
  workflow_dispatch:

jobs:
  component-test:
    runs-on: ubuntu-latest
    services:
      mongodb:
        image: mongo
        options: >-
          --health-cmd mongo
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 27017:27017
    steps:
    - uses: actions/checkout@v2
    - name: component test
      run: |
        cd ./api/
        npm install
        npm run start &
        cd ..
        npm install
        npm install -g jest-cli
        npm test
