import React, { Component } from 'react'
import './_style.css';

export default class Loading extends Component {
  render() {
    return (
        <div className="loader" id="centered">Loading...</div>
    )
  }
}